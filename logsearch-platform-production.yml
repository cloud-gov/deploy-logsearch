instance_groups:
- name: kibana
  jobs:
  - name: oauth2-proxy
    properties:
      redirect_url: https://logs-platform.fr.cloud.gov/oauth2/callback
- name: ingestor
  jobs:
  - name: ingestor_syslog
    properties:
      logstash_ingestor:
        health:
          timeout: 900

